PORT=3000

# Local PostgreSQL
DB_USER=postgres
DB_HOST=localhost
DB_PASSWORD=
DB_NAME=mi_app_db
DB_PORT=5432

# Production (Supabase): use Session pooler URI from Dashboard → Connect
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# Comma-separated origins for CORS (e.g. https://tu-app.vercel.app)
# ALLOWED_ORIGINS=
